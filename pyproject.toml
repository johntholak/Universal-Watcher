[project]
name = "automated-job-hunter"
version = "0.0.1"
description = "Bootstrap framework for a standalone personal job discovery and prioritization agent."
requires-python = ">=3.12"
dependencies = []

[tool.unittest]
start-directory = "tests"
